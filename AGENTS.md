# Repository Instructions

- Keep Windows configuration under `win/` and WSL configuration under `wsl/`.
- Keep `mise.toml` and `mise.lock` synchronized when changing tools.
- Run `mise run check --lint` before publishing changes.
